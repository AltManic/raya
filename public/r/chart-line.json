{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "chart-line",
  "title": "Chart: Line",
  "description": "Trend line chart wrapper. Legend/grid visibility are System knobs.",
  "dependencies": [
    "recharts"
  ],
  "files": [
    {
      "path": "registry/default/ui/chart-line.tsx",
      "content": "import { LineChart, Line, XAxis, YAxis, CartesianGrid, ResponsiveContainer } from \"recharts\"\nimport type { CSSProperties, ReactNode } from \"react\"\nimport { ChartLegend, ChartTip, chartAxisProps, chartGridProps, type ChartSeries } from \"./chart-shared\"\n\nconst cx = (...parts: unknown[]) => parts.filter((p): p is string => typeof p === \"string\").join(\" \")\n\nconst scopeVars = {\n  \"--raya-chart-legend-display\": \"var(--raya-chart-line-legend-display, none)\",\n  \"--raya-chart-grid-visibility\": \"var(--raya-chart-line-grid-visibility, visible)\",\n} as CSSProperties\n\nexport interface LineChartProps {\n  data: Array<Record<string, unknown>>\n  xKey: string\n  series: ChartSeries[]\n  height?: number\n  xLabels?: ReactNode\n  className?: string\n}\n\nexport function LineChartWrapper({ data, xKey, series, height = 240, className }: LineChartProps) {\n  return (\n    <div data-slot=\"chart\" className={cx(\"raya-chart w-full flex flex-col gap-3\", className)} style={{ ...scopeVars, height }}>\n      <ChartLegend series={series} />\n      <ResponsiveContainer width=\"100%\" height=\"100%\">\n        <LineChart data={data} margin={{ top: 8, right: 8, bottom: 0, left: 0 }}>\n          <CartesianGrid vertical={false} {...chartGridProps} />\n          <XAxis dataKey={xKey} {...chartAxisProps} />\n          <YAxis width={44} {...chartAxisProps} />\n          <ChartTip />\n          {series.map((s, i) => (\n            <Line\n              key={s.key}\n              type=\"monotone\"\n              dataKey={s.key}\n              name={s.label}\n              stroke={`var(${s.colorVar ?? `--chart-${(i % 5) + 1}`})`}\n              strokeWidth={2}\n              dot={false}\n              activeDot={{ r: 4 }}\n            />\n          ))}\n        </LineChart>\n      </ResponsiveContainer>\n    </div>\n  )\n}\n",
      "type": "registry:ui"
    },
    {
      "path": "registry/default/ui/chart-shared.tsx",
      "content": "import { Tooltip as RechartsTooltip } from \"recharts\"\nimport type { ReactNode } from \"react\"\n\nexport interface ChartSeries {\n  key: string\n  label: string\n  colorVar?: string\n}\n\nexport function ChartLegend({ series }: { series: ChartSeries[] }) {\n  return (\n    <div className=\"raya-chart-legend\">\n      {series.map((s) => (\n        <span key={s.key} className=\"raya-chart-legend-item\">\n          <span\n            className=\"raya-chart-legend-swatch\"\n            style={{ \"--swatch\": `var(${s.colorVar ?? \"--chart-1\"})` } as Record<string, string>}\n          />\n          {s.label}\n        </span>\n      ))}\n    </div>\n  )\n}\n\nexport const chartAxisProps = {\n  stroke: \"var(--muted-foreground)\",\n  tickLine: false,\n  axisLine: false,\n  fontSize: 11,\n} as const\n\nexport const chartGridProps = {\n  stroke: \"var(--border)\",\n  strokeDasharray: \"3 3\",\n} as const\n\nexport function ChartTip() {\n  return (\n    <RechartsTooltip\n      cursor={{ stroke: \"var(--border)\" }}\n      content={\n        (({ active, payload, label }: { active?: boolean; payload?: Array<{ name?: ReactNode; value?: ReactNode; color?: string }>; label?: ReactNode }) =>\n          active && payload && payload.length > 0 ? (\n            <div className=\"raya-chart-tooltip\">\n              {label != null && <div style={{ fontWeight: 600 }}>{label}</div>}\n              {payload.map((entry, i) => (\n                <div key={i} style={{ display: \"flex\", gap: \"0.5rem\", alignItems: \"center\" }}>\n                  <span style={{ width: 8, height: 8, borderRadius: 2, background: entry.color }} />\n                  <span>{entry.name}</span>\n                  <span style={{ marginLeft: \"auto\", fontVariantNumeric: \"tabular-nums\" }}>{entry.value}</span>\n                </div>\n              ))}\n            </div>\n          ) : null) as never\n      }\n    />\n  )\n}\n",
      "type": "registry:ui"
    }
  ],
  "docs": "Requires the @raya/core item. Knob values come from your System's stylesheet.",
  "type": "registry:ui"
}
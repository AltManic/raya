{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "button",
  "title": "Button",
  "description": "Primary action element with variant and size driven by the System.",
  "dependencies": [
    "@base-ui/react"
  ],
  "files": [
    {
      "path": "registry/default/ui/button.tsx",
      "content": "import { Button as ButtonPrimitive } from \"@base-ui/react/button\"\nimport type { ComponentProps } from \"react\"\n\nconst cx = (...parts: unknown[]) => parts.filter((p): p is string => typeof p === \"string\").join(\" \")\n\nexport function Button({ className, ...props }: ComponentProps<typeof ButtonPrimitive>) {\n  return (\n    <ButtonPrimitive\n      data-slot=\"button\"\n      className={cx(\"raya-button\", className)}\n      {...props}\n    />\n  )\n}\n",
      "type": "registry:ui"
    }
  ],
  "docs": "Requires the @raya/core item. Set `data-raya` on <html> or a wrapper to activate a System.",
  "type": "registry:ui"
}